# Copy this to .env and fill in your key
ANTHROPIC_API_KEY=your_anthropic_api_key_here
PORT=3001

# Shared secret for POST /api/admin/delete-user ("forget me"). Send it as the
# X-Admin-Key header. No value set = the route is disabled (401 on every call).
ADMIN_API_KEY=set_a_long_random_value_here
